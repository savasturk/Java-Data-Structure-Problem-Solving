In this solution, it is checked whether { [ ( opening braces } ] ) matches the closing parenthesis in the string.

It was developed in the Java environment. 

It has a solution with both Stack and Regex.